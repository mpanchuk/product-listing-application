== Product Listing Application

This is a test Application 

To setup Application:

1. Make sure you have instaled PostgreSQL

2. Run 'bundle install'

3. Create database.yml file with your credentials (use database.yml.example as an example)

4. Run 'rake db:create db:migrate'

5. Run rails server